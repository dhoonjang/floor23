import React from 'react';
import { SMenu } from '../styleComponent.js';

const Film = () => {
    return (
      <div>
        <SMenu>
            Photo
        </SMenu>
        <SMenu>
            Video
        </SMenu>
      </div>
    );
};

export default Film;
